name: building-blocks
on:
  push:
  workflow_dispatch:

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
    - name: Say Hello
      run: echo "Hello World"

  echo-goodbye:
    runs-on: ubuntu-latest
    steps:
    - name: failed steps
      run: |
        "I will fail"
        exit 1
    - name: Say Goodbye
      run: echo "Goodbye"
